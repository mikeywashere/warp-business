# TEMPLATE FILE - Copy to secrets.yaml and fill in real values
# Generate base64: echo -n "your-value" | base64
# NEVER commit secrets.yaml to source control (it is in .gitignore)
apiVersion: v1
kind: Secret
metadata:
  name: warp-secrets
  namespace: warp-business
type: Opaque
data:
  postgres-user: REPLACE_WITH_BASE64_ENCODED_VALUE
  postgres-password: REPLACE_WITH_BASE64_ENCODED_VALUE
  jwt-key: REPLACE_WITH_BASE64_ENCODED_VALUE
  keycloak-admin-password: REPLACE_WITH_BASE64_ENCODED_VALUE
