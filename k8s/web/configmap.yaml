apiVersion: v1
kind: ConfigMap
metadata:
  name: warp-web-config
  namespace: warp-business
data:
  ASPNETCORE_ENVIRONMENT: Production
  ASPNETCORE_URLS: "http://+:8080"
  # Aspire service discovery — points Web at the API K8s service
  services__api__http__0: "http://warp-api:8080"
